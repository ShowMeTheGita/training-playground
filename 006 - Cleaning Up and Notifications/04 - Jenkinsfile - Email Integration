pipeline {
    agent { 
		docker { 
			image 'alpine:latest' 
		}
	}
    stages {
        stage('build') {
            steps {
                sh ""
            }
        }
    }
    post {
    	emailext body: 'Pipeline run successful', recipientProviders: [guilherme.ferreira.silva@celfocus.com], subject: 'Pipeline run success'
    }
}
